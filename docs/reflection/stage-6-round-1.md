# Stage 6 Reflection — Round 1

Review date: 2026-07-22. Scope: document versions, parsing, chunks, citations, lexical/vector
ports, retrieval persistence, read-only surfaces, PostgreSQL and offline safety.

| ID | Role | Severity | Description and evidence | Affected files | Resolution | Blocking | Status |
|---|---|---|---|---|---|---|---|
| S6-R1-001 | RAG architect | HIGH | The first implementation built postings and BM25 but had no executable lexical-search composition. The missing import test failed before the search service existed. | `domain/retrieval/lexical.py`, `service.py` | Added bounded exact-token BM25 search, stable ties and `DeterministicRetrievalEngine`; LEXICAL=PASS, VECTOR=BLOCKED, lexical-only HYBRID=PARTIAL. | Yes | FIXED |
| S6-R1-002 | Tool/Agent/MCP architect | HIGH | Seven of eight RAG handlers returned one fixed placeholder instead of reading persisted state. | `tools/rag.py`, `api/routes/rag.py`, `db/repositories/knowledge.py` | Added bounded persisted document/chunk/citation/run/evidence reads; search remains fingerprinted cache-only. | Yes | FIXED |
| S6-R1-003 | Reliability engineer | HIGH | CLI parse/index/search commands were unconditional BLOCKED stubs and could not execute when approved bytes existed. | `cli_documents.py`, `cli_rag.py` | Added explicit DB-backed version registration, parsing, chunk/citation persistence, lexical build, Retrieval Run creation and bounded inspection commands. | Yes | FIXED |
| S6-R1-004 | Database architect | HIGH | A completed Parse Run stored only the canonical checksum, so exact parsed-document reuse could not reconstruct canonical text. A model test failed first. | `db/models/knowledge.py`, migration `0005`, repository | Persist canonical text on the immutable Parse Run, reconstruct pages/sections exactly, and retain the checksum. | Yes | FIXED |
| S6-R1-005 | Financial citation expert | HIGH | The initial SQL read path could label metadata checks as sufficient without checking original Blob bytes. | `db/repositories/knowledge.py`, `api/dependencies.py`, `cli_rag.py` | Injected read-only BlobStorage and verify byte checksum, MIME, size, canonical text and excerpt deterministically. Missing Blob context remains honestly BLOCKED. | Yes | FIXED |
| S6-R1-006 | Financial citation expert | HIGH | The first SQL Evidence Bundle joined persisted citations without re-verifying them. | `db/repositories/knowledge.py` | Every persisted item is deterministically verified; invalid/unavailable citations are excluded and a PASS bundle degrades to PARTIAL with `INVALID_CITATIONS_EXCLUDED`. | Yes | FIXED |
| S6-R1-007 | Database/reliability engineer | HIGH | Stage 2–5 migration fixtures reset only old tables; Stage 6 tables/functions survived and caused duplicate-table failures. | PostgreSQL integration fixtures | Validated test databases now reset the isolated `public` schema before replaying the full migration chain. | Yes | FIXED |
| S6-R1-008 | Security engineer | MEDIUM | A manual rerun initially targeted port 5432 rather than the project-owned loopback port 55432, producing connection timeouts. | Verification procedure | Corrected every final command to the documented 55432 URLs and terminated orphan pytest children before rerun. | No | FIXED |
| S6-R1-009 | API architect | HIGH | Search/list scope errors could become HTTP 500, detail misses returned HTTP 200, and citation verification accepted no as-of scope. | `api/routes/rag.py`, Tool schemas, API contracts | Validate exactly one aware snapshot/as-of scope before model construction; use safe 422 and 404 envelopes. | Yes | FIXED |
| S6-R1-010 | Financial citation expert | HIGH | Superseded versions and Citation anchors were not fully evaluated against historical cutoff and exact Snapshot membership. | citation verifier, lexical eligibility, migration | Added immutable supersession relation validation, cutoff-aware exclusion and exact-version/Snapshot historical access. | Yes | FIXED |
| S6-R1-011 | RAG architect | HIGH | Retrieval fingerprints omitted active index generations and BM25 document frequency was computed before eligibility filtering. | retrieval service, lexical search, SQL cache lookup | Fingerprints now bind lexical/vector generation plus fusion/reranker versions; BM25 statistics use only eligible chunks; cache lookup requires the current compatible index. | Yes | FIXED |
| S6-R1-012 | Reliability engineer | HIGH | Concurrent parse/retrieval callers could duplicate artifacts, and version reuse could bypass Blob/policy comparison after a uniqueness race. | repositories, identity/parse/retrieval services, PostgreSQL tests | Added explicit acquisition ownership, loser in-progress outcomes and post-race content/policy revalidation. | Yes | FIXED |
| S6-R1-013 | Financial citation expert | HIGH | Production verification duplicated incomplete checks and Evidence Bundles omitted key provenance/version fields. | citation verifier, SQL repository, evidence schemas | SQL delegates to the deterministic verifier; bundles include trust, publication, provider/source, locator and parser/tokenizer/citation generations. | Yes | FIXED |
| S6-R1-014 | Parser/RAG architect | HIGH | Chunking flattened PDF pages and HTML/JSON sections into generic text offsets, losing native locations. | chunker, parsers, chunk/citation persistence | Chunking is page/section first and retains PDF page, HTML anchor or JSON pointer plus exact offsets; PostgreSQL integration verifies pointer persistence. | Yes | FIXED |
| S6-R1-015 | Financial citation expert | HIGH | Excerpts were checked against the whole canonical document rather than the claimed page/section, and Citation lacked Page/Section foreign keys. | verifier, models, migration | Excerpt ranges must be contained by the claimed page/section; page/section RESTRICT FKs and a lineage trigger bind the exact parse generation. | Yes | FIXED |
| S6-R1-016 | RAG architect | HIGH | Retrieval Hits permitted null or mismatched citations, so a PASS run could yield uncited or silently empty evidence. | retrieval schemas/service, models, migration, bundle builder | Citation is required, a composite Citation–Chunk FK prevents mismatch, execution verifies citations before writes, and invalid/empty evidence degrades explicitly. | Yes | FIXED |
| S6-R1-017 | Financial master-data architect | HIGH | A caller could attach one security's SourceDocument bytes to another security's logical identity. | identity service, SQL repository, migration | Service checks logical-document security and a PostgreSQL lineage trigger binds logical identity, SourceDocument, provider and payload to the same version. | Yes | FIXED |
| S6-R1-018 | Reliability engineer | HIGH | A uniqueness-race winner with the same checksum but incompatible trust/origin/live policy could be accepted as CREATED. | identity service, repository protocol, concurrency tests | `acquire_version` reports ownership and every winner is rechecked for exact content metadata and evidence policy before CREATED/REUSED. | Yes | FIXED |

Unresolved CRITICAL: 0. Unresolved HIGH: 0. Synthetic fixtures never become company evidence,
and no semantic-quality claim is made for TEST_ONLY vectors.
