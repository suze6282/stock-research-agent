# Stage 8 development Reflection — Round 1

Date: 2026-07-29
Branch: `stage-8/verifiable-report-reflection`
Scope: Tasks 0–45, including real PostgreSQL and three acceptance flows.

| ID | Role | Severity | Description | Evidence | Affected files | Fix | Blocking | Status |
|---|---|---|---|---|---|---|---|---|
| S8-R1-001 | Report architecture | HIGH | The CLI command surface exists, but its default `application_factory` raises `REPORT_APPLICATION_NOT_CONFIGURED`; installed commands cannot reach the database workflows. | Direct source review and `test_report_cli.py` only replaces the factory with a fake. | `src/stock_research_agent/cli_reports.py`, `src/stock_research_agent/report_cli_application.py`, `tests/integration/test_report_repository_postgres.py` | Added a production composition root with one transaction per operation, explicit seed/read/write routing, rollback on domain validation failure, and PostgreSQL integration coverage. | YES | FIXED |
| S8-R1-002 | Financial research | LOW | Real-company output correctly contains identity and limitations only; it must remain explicit that no financial analysis was completed. | Industrial FII and Micron acceptance tests each persist zero document Citation inputs and only one identity Claim/Evidence pair. | `tests/integration/test_report_industrial_fii.py`, `tests/integration/test_report_micron.py` | Preserve the current honest-degradation assertions and stage report wording. | NO | VERIFIED |
| S8-R1-003 | Citation and evidence | HIGH | `ResearchReportAggregateWrite` contains only the report; composition and `add_report` do not atomically carry Claim/Evidence/Citation binding rows. A factual block can therefore be stored without the required database lineage. | Focused composition, revision, release and PostgreSQL tests now cover the complete graph and reject factual persistence without bindings. | `domain/reports/reporting.py`, `domain/reports/composition.py`, `domain/reports/revision.py`, `domain/reports/release_gate.py`, `db/repositories/reports.py` | Added an immutable persistence aggregate, exact factual coverage validation, atomic binding persistence, and deterministic rebasing for revised/sealed versions. | YES | FIXED |
| S8-R1-004 | Runtime Reflection | MEDIUM | Synthetic marker evaluation originally used `set.issubset(str)`, which can never validate multi-character markers. | Focused Synthetic RED reproduced an unresolved `SYNTHETIC_NOT_REAL_COMPANY_RESEARCH` Finding. | `domain/reports/reflection.py`, `test_report_synthetic_flow.py` | Replaced with deterministic per-marker substring checks; keep regression. | NO | FIXED |
| S8-R1-005 | Revision | LOW | The fixed one-round Revision registry is closed and cannot create facts; unknown remediations remain unresolved. | 21 Revision tests plus the Synthetic Round1→Revision→Round2 flow pass. | `domain/reports/revision.py` | No code change; retain regression coverage. | NO | VERIFIED |
| S8-R1-006 | Release Gate | MEDIUM | The original Gate rejected every Synthetic context even when correctly isolated, preventing the approved engineering-only PUBLISHABLE flow. | Synthetic RED returned `PARTIAL` with `NO_SYNTHETIC_CONTAMINATION`. | `domain/reports/release_gate.py`, `test_report_synthetic_flow.py` | Permit exact Synthetic mode/status pairs only when deterministic marker checks have no Finding. | NO | FIXED |
| S8-R1-007 | Security | LOW | Model ports are correctly blocked and template/provider injection cases fail closed. | 76 security/provider/template/Reflection tests pass; provider source has no network/model/environment runtime. | `domain/reports/providers.py`, `domain/reports/templates.py` | No change; keep production model providers BLOCKED and token consumption zero. | NO | VERIFIED |
| S8-R1-008 | PostgreSQL | HIGH | Initial ORM/migration binding columns disagreed with approved domain enums and locator/reference fields. | Real migration inspection showed `CONFLICT`/`CIT` values and missing visible/source fields; the corrected model, migration and repository now share the same fields and enums. | `0007_create_verifiable_reports_and_reflection.py`, `db/models/reports.py`, `db/repositories/reports.py`, `test_report_repository_postgres.py` | Aligned migration and ORM and added real PostgreSQL rejection/persistence coverage. | YES | FIXED |
| S8-R1-009 | Fixture and cross-platform | LOW | Report fixtures have LF attributes, exact Git Blob/worktree/manifest checksums and no CRLF. | `test_report_fixture_integrity.py` passes 3 checks. | `.gitattributes`, `tests/fixtures/reports` | No change; never renormalize unrelated paths. | NO | VERIFIED |
| S8-R1-010 | Testing reliability | HIGH | The Synthetic “full flow” currently imports private unit-test builders and does not exercise the production composition and binding path. | The replacement flow uses an independent neutral Synthetic builder, production composition, appendix construction, binding rebasing, two Reflection rounds, Revision and Release Gate; repository and CLI transactions are covered against PostgreSQL. | `tests/support/report_scenarios.py`, `test_report_synthetic_flow.py`, `test_report_repository_postgres.py` | Removed all private unit-helper imports and connected the production Claim Index/Evidence Appendix and binding graph to the full Synthetic flow. | YES | FIXED |

Unresolved `CRITICAL`: 0
Unresolved `HIGH`: 0

Round 2 is prohibited until every unresolved HIGH item has a focused regression,
minimum fix and affected-suite rerun.
